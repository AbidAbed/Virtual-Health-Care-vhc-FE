function useDoctorBioValidator(value) {
    if (!value || !value.trim()) {
      return "Doctor's bio is required";
    }


  
    // Add additional validation criteria if needed
  
    return null;
  }
  
  export default useDoctorBioValidator;
  